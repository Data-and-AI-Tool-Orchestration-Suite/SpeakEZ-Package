# USE_GPU=1 to use GPU, 0 to use CPU
USE_GPU=

# HuggingFace token with access to the gated pyannote models
# (pyannote/speaker-diarization-3.1, pyannote/segmentation-3.0, pyannote/wespeaker-voxceleb-resnet34-LM)
HF_TOKEN=